[project]
name = "kresy-siberia-2kpl-recon"
version = "1.0.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.18.4",
    "playwright>=1.58.0",
    "poethepoet>=0.44.0",
    "polars>=1.39.3",
    "sqlalchemy>=2.0.49",
    "typer>=0.24.1",
]

[tool.setuptools.package-dir]
"" = "src"

[project.scripts]
recon = "recon.cli.commands:app"